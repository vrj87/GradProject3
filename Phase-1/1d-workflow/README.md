# Phase 1d — reviewer workflow

Runs 1b → 1a → 1c, writes the artefact bundle and HTML report, and serves the Myntra storefront.

```bash
cd Phase-1
npm run 1d
npm run dev       # http://localhost:3000
```

Exit: reviewer can open Live Insights and inspect `data/discovery/`.
