# LLM (optional — pipeline uses rule-based + hybrid fallback if both are empty)
GROQ_API_KEY=
OPENAI_API_KEY=

# LLM batch size is auto-tuned; Groq llama-3.3-70b-versatile is primary, gpt-4o-mini fallback

# Optional live sources
YOUTUBE_API_KEY=

# Collect UI
COLLECT_PORT=3001
REPORT_PORT=3001
