# Phase 5 — copy to .env. Never commit real keys.

# 5a
DATABASE_URL="file:./dev.db"
NEXT_PUBLIC_APP_URL="http://localhost:3100"

# 5b — public product page fetch. Only myntra.com is allowlisted.
PRODUCT_INGEST_TIMEOUT_MS="8000"

# 5c — provider priority is Groq, then OpenAI, then the rule-based coach.
# Leave both blank: every flow still works offline on the rule-based tier.
GROQ_API_KEY=""
OPENAI_API_KEY=""

# 5e
N8N_WEBHOOK_SECRET=""
NEXT_PUBLIC_COLLECT_URL="http://localhost:3001"
